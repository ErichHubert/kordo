# Local Infrastructure

This directory will hold Docker Compose configuration for local development.

Milestone 1 only creates the directory and documents the intended location. The
PostgreSQL service is added when the control-plane stub starts persisting runs.
