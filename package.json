{
  "name": "kordo",
  "version": "0.1.0",
  "description": "Reusable workflow execution platform for agent-driven runs.",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=22.12.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "build": "corepack pnpm -r --if-present build",
    "dev": "corepack pnpm --parallel --filter @kordo/control-plane --filter @kordo/runner dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "smoke:local": "corepack pnpm --filter @kordo/control-plane smoke:local",
    "test": "corepack pnpm --filter @kordo/contracts build && corepack pnpm --filter @kordo/policy build && corepack pnpm -r --if-present test",
    "typecheck": "corepack pnpm --filter @kordo/contracts build && corepack pnpm --filter @kordo/policy build && corepack pnpm -r --if-present typecheck",
    "verify": "corepack pnpm build && corepack pnpm typecheck && corepack pnpm test && corepack pnpm lint && corepack pnpm format:check"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^24.10.1",
    "eslint": "^9.39.1",
    "prettier": "^3.7.4",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.4",
    "vitest": "^4.0.14"
  }
}
